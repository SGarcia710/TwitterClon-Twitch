import React from 'react';
import { View, Text } from 'react-native';

const Lists = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>Lists</Text>
    </View>
  );
};

export default Lists;
