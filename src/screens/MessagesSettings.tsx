import React from 'react';
import { View, Text } from 'react-native';

const MessagesSettings = () => {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: 'center',
        alignItems: 'center',
      }}
    >
      <Text>MessagesSettings</Text>
    </View>
  );
};

export default MessagesSettings;
